{
  "name": "venus",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "npx patch-package",
    "postJb":"react-native bundle --entry-file index.js --platform ios --dev false --bundle-output ios/main.jsbundle --assets-dest ios",
    "android": "react-native run-android",
    "clean:android": "rm -rf android/app/build",
    "clean:ios": "rm -rf ios/build",
    "clean:node": "rm -rf node_modules && yarn --frozen-lockfile",
    "clean": "yarn clean:ios && yarn clean:android && yarn clean:node",
    "ios": "react-native run-ios",
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "react": "18.0.0",
    "react-native": "0.69.2"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@babel/runtime": "^7.12.5",
    "@react-native-community/eslint-config": "^2.0.0",
    "babel-jest": "^26.6.3",
    "eslint": "^7.32.0",
    "jest": "^26.6.3",
    "metro-react-native-babel-preset": "^0.70.3",
    "react-test-renderer": "18.0.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
